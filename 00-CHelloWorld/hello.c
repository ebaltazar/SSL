/* Hello World!
 * Para compilar y generar salida: gcc -Wall -std=c11 hello.c -o -output.txt
 * Al generar el archivo output.txt arroja el siguiente mensaje dentro: 'This program cannot be run in DOS mode'
 * Por lo que luego de generarlo utilizo: output.txt > salida.txt
 * No pude encontrar forma de generarlo directamente.
 */

#include <stdio.h>
#include <conio.h>

int main()
{
    printf("Hola Mundo!\n");
    return 0;
}
